import React from 'react';
import { Flex } from '../atoms';
import LocationModal from '../organisms/LocationModal';

const Home = ({

}) => {
  return (
    <Flex>
      <LocationModal />
    </Flex>
  )
}

export default Home;